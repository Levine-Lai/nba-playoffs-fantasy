# Project Context | 项目上下文
This file is the cross-conversation memory for the repo. Keep it concise, current, and bilingual. / 这是仓库的跨会话记忆文件，需要保持精炼、最新，并且中英双语。

## Product Shape | 产品形态
- Frontend lives in `frontend/` and deploys on Vercel. / 前端位于 `frontend/`，部署到 Vercel。
- Backend lives in `backend/` and runs on Cloudflare Workers + D1. / 后端位于 `backend/`，运行在 Cloudflare Workers + D1。
- Shared business rules should stay reusable instead of leaking runtime-specific logic across apps. / 共享业务规则要保持可复用，不要把运行时专属逻辑混到另一侧。
- The product is an NBA playoff-only fantasy game with 10-player rosters. / 这是一个仅覆盖 NBA 季后赛的 fantasy 产品，每队 10 人阵容。

## Core Routes | 核心路由
- User-facing leaderboard language is `Standing`, not `League`. / 面向用户的排行榜文案使用 `Standing`，不用 `League`。
- Main leaderboard page is `/standing`; legacy `/leagues` is redirect-only. / 主排行榜页面是 `/standing`；旧的 `/leagues` 只保留跳转。
- `/points` is the per-manager daily scoring page with the roster view and right sidebar snapshot. / `/points` 是单个经理的每日得分页，包含阵容展示和右侧资料快照。
- `/schedule` uses April, May, and June month calendars as the main view, plus `Playoff Path` and a matchup detail modal. / `/schedule` 以 4、5、6 月月历为主视图，同时展示 `Playoff Path` 和比赛详情弹窗。

## Live Rules | 当前玩法规则
- `Day 1` is the real playoff opener on `2026-04-18`. / `Day 1` 是真实季后赛揭幕日 `2026-04-18`。
- `Points` stay hidden until the `Day 1` deadline passes. / `Points` 会在 `Day 1` 截止前保持隐藏。
- Before the `Day 1` deadline, transfers are unlimited setup moves and do not consume playoff FT or `Total transactions`. / `Day 1` 截止前的转会属于无限次建队调整，不消耗季后赛 FT，也不计入 `Total transactions`。
- After the `Day 1` deadline, each team gets `8` total playoff FT and every extra normal transfer costs `-50`. / `Day 1` 截止后，每队整个季后赛共有 `8` 次 FT，额外普通转会每次扣 `-50`。
- `Wildcard` and `All-Star` unlock only after the `Day 1` deadline, with one of each per manager for the full playoff run. / `Wildcard` 和 `All-Star` 只会在 `Day 1` 截止后解锁；每位经理整个季后赛各有一张。
- Play-in games (`005...`) are excluded from schedule, scoring, and standings. / 附加赛（`005...`）不计入赛程、得分和排行榜。
- Unstarted playoff games marked `ifNecessary` by the NBA feed are treated as tentative placeholders and excluded from schedule display, player upcoming schedules, editable/scoring day grouping, and live seed generation until the feed confirms them or they start. / NBA feed 中标记为 `ifNecessary` 且尚未开赛的季后赛比赛视为临时占位，暂不进入赛程展示、球员未来赛程、可编辑/计分日分组和 live seed；直到官方确认或比赛开赛后再纳入。
- Player Selection market queries show selectable players from teams still alive in the playoff run: teams with a confirmed upcoming matchup and teams that have already clinched a series while waiting for the next matchup to appear in the NBA feed. Roster hydration by existing player ids/names/codes stays unfiltered so managers who already own eliminated/non-playoff players keep seeing their lineups. / Player Selection 的市场查询会展示仍在季后赛中的球队里的可选球员：包括已有官方下一场赛程的球队，也包括已经赢下系列赛、正在等待 NBA feed 生成下一轮赛程的提前晋级球队。按现有球员 id/name/code hydrate 阵容的路径不做该过滤，因此已经持有淘汰/非季后赛球员的经理仍能正常看到自己的阵容。
- Fantasy scoring is `PTS x1 + REB x1 + AST x2 + STL x3 + BLK x3 - TOV x1`. / Fantasy 计分规则是 `PTS x1 + REB x1 + AST x2 + STL x3 + BLK x3 - TOV x1`。
- Effective scoring counts up to 5 active players and must end in a valid `3BC + 2FC` or `2BC + 3FC` shape, using starters first and then bench order to fill gaps. / 有效计分最多统计 5 名有比赛的球员，并且最终必须满足 `3BC + 2FC` 或 `2BC + 3FC` 的合法阵型，优先使用首发，再按替补顺序补位。
- For gamedays on or after `2026-04-20`, a player who still has not appeared by Beijing `16:00` is treated as a non-participant for that gameday, so later bench players may replace them. / 对于 `2026-04-20` 及之后的 gameday，如果球员到北京时间 `16:00` 仍未实际出场，则该球员在该日视为未上场，后续替补可以递补。

## UI Notes | UI 约定
- Standing highlights the logged-in user with a deeper blue row that stays highlighted on hover. / Standing 会用更深的蓝色高亮当前登录用户，并在 hover 时保持高亮。
- On another manager's `/points` page, the right sidebar must show the viewed manager's snapshot, not the current viewer's own profile. / 查看其他经理的 `/points` 页面时，右侧栏必须显示被查看经理的快照，而不是当前登录者自己的资料。
- Effective scoring players on `/points` are highlighted in yellow, and that highlight must stay aligned with the real scoring logic. / `/points` 里的有效计分球员会用黄色高亮，并且必须和真实计分逻辑一致。
- Fantasy scores render as whole numbers with no decimals across Standing, Points, sidebars, and Home leaders. / Standing、Points、侧栏和 Home leaders 中的 fantasy 分数都显示为整数，不显示小数。
- `Daily Fantasy Leaders` belongs at the bottom of `/points`, after the roster sections, not directly under the gameday summary card. / `Daily Fantasy Leaders` 应该放在 `/points` 页最底部、阵容区块之后，而不是直接放在当日总分卡下面。
- Home leaders score rendering should fall back to `entry.player.points` when `entry.points` is missing so the UI does not show an empty score band. / Home leaders 的分数渲染在 `entry.points` 缺失时要回退到 `entry.player.points`，避免分数条出现空白。

## Data And Sync Notes | 数据与同步说明
- `/api/standings` returns fresh cache immediately, but when the requested/selected cache is missing or expired it starts the de-duplicated all-phase refresh and waits up to `15s` for the rebuilt cache before falling back to a stored DB snapshot. / `/api/standings` 会立即返回新鲜缓存；但当请求的/选中的缓存缺失或过期时，会启动去重后的全 phase 刷新，并最多等待 `15s` 获取重建后的缓存，拿不到时才回退到数据库快照。
- Standing polling cadence comes from backend response fields `refreshIntervalMs` and `nextRefreshAt`; stale/snapshot fallbacks must schedule a short `nextRefreshAt` retry and clear live polling for that fallback response so the browser actually retries quickly. / Standing 的轮询节奏来自后端返回的 `refreshIntervalMs` 和 `nextRefreshAt`；当回退到过期缓存或快照时，必须设置较短的 `nextRefreshAt` 重试，并清掉该回退响应上的实时轮询间隔，确保浏览器真的快速重拉。
- Current scoring-day lineups must lock at the deadline; post-deadline reorders should affect only the next editable day. / 当前计分日的阵容必须在截止时锁定；截止后的调序只能影响下一可编辑日。
- Backend lineup locks are stored in `app_state` under `lineup_locks_v1`, with optional manual overrides in `lineup_corrections_v1`. / 后端阵容锁存放在 `app_state` 的 `lineup_locks_v1` 中，可选人工修正放在 `lineup_corrections_v1`。
- `GET/PUT /api/lineup` must use the active `All-Star` lineup snapshot when that chip is live for the current editable period. / 当当前可编辑周期存在生效中的 `All-Star` 时，`GET/PUT /api/lineup` 必须读写对应的激活阵容快照。

## Risks And Workflow | 风险与流程
- Any change that could affect live data or player progress must be surfaced before implementation and requires explicit approval; pure UI changes are exempt. / 任何可能影响线上数据或玩家进度的修改，都必须先说明影响并获得明确批准；纯 UI 修改除外。
- In this local Windows workspace, `rg` may fail with `Access is denied`; when that happens, use PowerShell-native `Get-ChildItem` and `Select-String` directly instead of retrying `rg`. / 在这个本地 Windows 工作区里，`rg` 可能会因为 `Access is denied` 失败；遇到时直接使用 PowerShell 原生的 `Get-ChildItem` 和 `Select-String`，不要重复尝试 `rg`。
- Do not push or deploy by default. Only run `git push` or deployment commands when the user explicitly asks. / 默认不要执行 `git push` 或部署命令；只有用户明确要求时才执行。
- Final responses for deployable changes must still end with the exact command block the user should run next. / 只要改动涉及可部署代码，最终回复仍必须以用户下一步应执行的精确命令块结尾。
- Frontend `npm audit --omit=dev` is improved but not fully clean after staying on the Next 14 line: `next@14.2.35` removes the previous critical finding, but npm still reports a high-severity Next/PostCSS chain whose automated fix is the breaking `next@16.2.4` upgrade. Treat that as a separate migration task. / 前端在保持 Next 14 线后，`npm audit --omit=dev` 已改善但尚未完全清零：`next@14.2.35` 移除了此前的 critical 问题，但 npm 仍报告一条 high 级别的 Next/PostCSS 链，自动修复目标是破坏性升级到 `next@16.2.4`。应把它作为单独迁移任务处理。
- `backend/tmp/d1-seed.sql` is a generated temporary artifact and can be stale; always run a seed generation command immediately before any `db:seed:apply:*` command. / `backend/tmp/d1-seed.sql` 是生成出来的临时产物，可能过期；每次执行任何 `db:seed:apply:*` 前都要先运行对应 seed 生成命令。

## Latest Change | 最新变更
- Standing rows now render larger team names and score text, and the table includes `FT Used` plus `Card Used` columns. Backend standing members include `freeTransfersUsed/freeTransfersLimit` and `cardsUsed`; FT Used counts normal post-deadline transfers including penalty transfers while excluding unlimited setup, Wildcard, and All-Star moves. Card badges show `WC` and `AS` in green when unused and red when used. No schema migration, seed change, new env var, or new dependency is required; deploy the Worker before the frontend. / Standing 行现在会用更大的队名和分数字号展示，并新增 `FT Used` 与 `Card Used` 两列。后端 Standing member 会返回 `freeTransfersUsed/freeTransfersLimit` 和 `cardsUsed`；FT Used 统计截止后普通转会次数，包括超额扣分转会，但不包括建队无限转会、Wildcard 和 All-Star 转会。卡片徽章显示 `WC` 和 `AS`，未使用为绿色，已使用为红色。本次不需要 schema migration、seed 变更、新环境变量或新依赖；发布时先部署 Worker，再部署前端。
- Standing cache misses/expired entries now wait up to `15s` for the shared all-member, all-phase refresh before returning, then fall back to a stored snapshot with a forced `5s` retry if the refresh is still running. `/points` fallback score reconciliation also invalidates `standing_payload_cache_v1` after updating `overall_points`. This fixes first-open Standing showing stale `user_states` scores until a manager page recalculates that player. No schema migration, seed change, new env var, or new dependency is required; deploy the Worker before the frontend. / Standing 缓存缺失或过期时，现在会最多等待 `15s` 让共享的全玩家、全 phase 刷新完成后再返回；如果刷新仍未完成，则回退到存储快照并强制 `5s` 后重试。`/points` 的兜底分数校准在更新 `overall_points` 后也会清掉 `standing_payload_cache_v1`。这修复了直接打开 Standing 时读取过期 `user_states` 分数，直到点进某个经理页单用户重算后才正常的问题。本次不需要 schema migration、seed 变更、新环境变量或新依赖；发布时先部署 Worker，再部署前端。
- Transfer penalties now sync into `league_points_ledger_v1` as `penalty:day:...` adjustment entries whenever Points/Profile/Standing refreshes write or reconcile scores. This fixes the bug where `history_json` recorded `cost < 0` transfers but cumulative `overall_points` and Standing totals omitted the deduction. `Gameday History` now returns `actualPoints` and `penaltyPoints`, and the UI displays penalty days as `actual(penalty)` such as `200(-50)` while the linked day preview still uses the net score. No schema migration, seed change, new env var, or new dependency is required; deploy the Worker before the frontend. / 转会扣分现在会在 Points/Profile/Standing 刷新写入或校准分数时，同步进 `league_points_ledger_v1`，形式为 `penalty:day:...` 调整项。这修复了 `history_json` 已记录 `cost < 0` 转会，但累计 `overall_points` 和 Standing 总分没有扣分的问题。`Gameday History` 现在返回 `actualPoints` 和 `penaltyPoints`，前端会把扣分日显示为 `实际得分(扣分)`，例如 `200(-50)`；点击进入当天预览时仍使用扣分后的净分。不需要 schema migration、seed 变更、新环境变量或新依赖；发布时先部署 Worker，再部署前端。
- `/points` right sidebar now includes a `transaction history` link inside `Transactions And Finance`, pointing to the viewed manager's new `/transactions/history?userId=...` page. The page lists every stored transfer with time, outgoing player, incoming player, and the chip card (`Wildcard` / `All-Star`) when the transfer was made under a card. Backend support is a read-only `GET /api/transactions/history` endpoint derived from existing `user_states.history_json`; no schema migration, seed change, new env var, or new dependency is required. Deploy the Worker before the frontend so production has the new API before the link is live. / `/points` 右侧栏现在会在 `Transactions And Finance` 里显示 `transaction history` 链接，指向被查看经理的新页面 `/transactions/history?userId=...`。该页面会列出已存储的每一笔转会，包括时间、转出球员、转入球员，以及转会发生在开卡状态下时对应的卡（`Wildcard` / `All-Star`）。后端支持是只读的 `GET /api/transactions/history`，数据来自现有 `user_states.history_json`；不需要 schema migration、seed 变更、新环境变量或新依赖。发布时先部署 Worker，再部署前端，避免链接上线时生产环境尚无新 API。
- Standing live refresh no longer persists incomplete official box score previews: live-data previews now carry `dataComplete` / `missingBoxScoreGameIds`, and `/api/standings`, `/api/points`, and `/api/profile` only write gameday points, ledger entries, and standing cache entries when the relevant box scores were available. This fixes intermittent Standing rows showing `0` for managers whose Points page already has real points after a single-user recalculation. No schema migration, seed change, env var, dependency, or manual release step is required. / Standing 实时刷新不再持久化不完整的官方 box score 预览：live-data preview 现在会携带 `dataComplete` / `missingBoxScoreGameIds`，`/api/standings`、`/api/points` 和 `/api/profile` 只有在相关 box score 已可用时才写入当日分数、ledger 和 Standing cache。这修复了部分经理在 Standing 偶发显示 `0`，但点进 Points 单用户重算后已有真实分数的问题。本次不需要 schema migration、seed 变更、环境变量、依赖或人工发布步骤。
- Frontend Next.js was upgraded from `14.2.5` to `14.2.35` as the safest same-major security patch found by `npm audit fix --dry-run`; the app still builds successfully. No new dependency, env var, schema migration, seed change, or manual release step was added. / 前端 Next.js 已从 `14.2.5` 升级到 `14.2.35`，这是 `npm audit fix --dry-run` 给出的同大版本内最安全补丁；应用仍可成功构建。本次没有新增依赖、环境变量、schema 迁移、seed 变更或人工发布步骤。
- Documentation and backend cleanup aligned the playoff FT rule to the current `8` total transfers everywhere checked: README, deployment docs, and PRD no longer mention the stale `6` value, and the unused legacy single-transfer helper with a hardcoded `-50` penalty was removed from backend gameplay code. This does not change runtime behavior, schema, seed data, env vars, or dependencies. / 文档和后端清理已把季后赛 FT 规则统一到当前生效的 `8` 次总额度：README、部署文档和 PRD 不再提到过时的 `6` 次；后端 gameplay 代码中带硬编码 `-50` 扣分的旧单笔转会辅助函数也已删除。本次不改变运行时行为、schema、seed 数据、环境变量或依赖。
- Points, Points History, and Standing now pass `managerName` instead of `gameId` into the shared display-name formatter, keeping identity fallback semantics aligned with the backend payload. This is UI-only and does not affect live data, schema, seed data, env vars, or dependencies. / Points、Points History 和 Standing 现在把 `managerName` 而不是 `gameId` 传给共享显示名称格式化函数，使身份兜底语义与后端返回字段保持一致。这是纯 UI 变更，不影响线上数据、schema、seed 数据、环境变量或依赖。
- `/points` now shows a `gameday history` link directly under the right-sidebar `Points / Rankings` block. The new `/points/history` page lists each started playoff Day with that manager's points, and each row links back to `/points?userId=...&phase=day-N` so the historical lineup and points reuse the existing day preview. Backend support is a read-only `GET /api/points/history` endpoint backed by `league_points_ledger_v1`; deploy the Worker before the frontend so production does not return backend `Not found.` for the new API. No schema migration, seed change, new env var, or new dependency is required. / `/points` 现在会在右侧栏 `Points / Rankings` 区块正下方显示 `gameday history` 链接。新的 `/points/history` 页面按已开始的季后赛 Day 列出该经理每天得分，每一行都会跳回 `/points?userId=...&phase=day-N`，从而复用现有的历史阵容和得分预览。后端支持是只读的 `GET /api/points/history`，数据来自 `league_points_ledger_v1`；发布时要先部署 Worker 再部署前端，避免生产环境的新 API 返回后端 `Not found.`。不需要 schema migration、seed 变更、新环境变量或新依赖。
- `Agents.md` now records the local Windows `rg` access-denied behavior and the PowerShell fallback (`Get-ChildItem` / `Select-String`) so future sessions do not repeat the failed search attempt. This is a docs/rules-only change with no deployable code, schema, seed, or env var impact. / `Agents.md` 现在记录了本地 Windows 环境中 `rg` 可能被拒绝访问的现象，以及对应的 PowerShell 兜底命令（`Get-ChildItem` / `Select-String`），避免后续新会话重复失败搜索。这是仅文档/规则变更，不影响可部署代码、schema、seed 或环境变量。
- Transactions no longer shows the long `Playoff FT remaining` explanatory sentence, and `/schedule` `Playoff Path` has been restyled into a stronger bracket path with fixed round lanes, series tags, connector lines, and clearer leading-team highlights. This is UI-only and does not affect live data or player progress. / Transactions 不再显示较长的 `Playoff FT remaining` 说明句；`/schedule` 的 `Playoff Path` 已优化为更强的晋级路径样式，包含固定轮次轨道、系列编号、连接线和更清晰的领先队伍高亮。这是纯 UI 变更，不影响线上数据或玩家进程。
- Playoff FT allowance is now `8` per team for the full playoff run. Migration `0006_playoff_free_transfers_to_eight.sql` updates `game_rules.weekly_free_transfers` and existing `user_states.weekly_free_limit` to `8`, while new-user defaults, seed/export scripts, backend fallbacks, and help copy now use the same value. / 季后赛 FT 额度现在是每队整个季后赛 `8` 次。迁移 `0006_playoff_free_transfers_to_eight.sql` 会把 `game_rules.weekly_free_transfers` 和现有 `user_states.weekly_free_limit` 更新为 `8`，同时新用户默认值、seed/export 脚本、后端兜底值和帮助页文案也统一使用该数值。
- `/schedule` Playoff Path now orders first-round series by true bracket adjacency: East `10/13` then `11/12`, West `14/17` then `15/16`, so each pair of adjacent first-round cards feeds the same next-round card. This is a display-only frontend change with no schema, seed, env var, or backend contract change. / `/schedule` 的 `Playoff Path` 现在按真实晋级相邻关系排列首轮系列：东部 `10/13` 后接 `11/12`，西部 `14/17` 后接 `15/16`，因此每两张相邻首轮卡片都会对应同一个下一轮卡片。这是纯前端展示变更，没有 schema、seed、环境变量或后端契约变更。
- Day 7 live scoring was repaired in remote D1 by replacing 14 stale `day:2026-04-24` ledger entries with freshly recomputed official-preview points, recalculating all managers' `overall_points`, `overall_rank`, and `total_players`, and deleting `standing_payload_cache_v1` so standings rebuild from the corrected ledger. `/points` historical preview now only labels real `history.cost < 0` transfers as transfer penalties, and current live previews no longer write an all-`PRE` zero into the long-term ledger. / Day 7 线上得分已在 remote D1 中修复：14 个过期的 `day:2026-04-24` ledger entry 已替换为重新计算的官方 preview 分数，并重算了所有经理的 `overall_points`、`overall_rank` 和 `total_players`，同时删除 `standing_payload_cache_v1` 让 standings 从修正后的 ledger 重建。`/points` 的历史 preview 现在只会把真正 `history.cost < 0` 的转会标记为 transfer penalties，当前实时 preview 也不会再把全部仍是 `PRE` 的 0 分写入长期 ledger。
- Points page `SQLITE_TOOBIG` risk is addressed by compacting `lineup_locks_v1` before writing it back to `app_state`: locked lineup snapshots now persist only scoring/replay essentials (`id`, `code`, team, position, salary, points window, roster value, bank) instead of display-heavy player assets such as headshots, logos, and upcoming schedule cells. Existing large lock registries remain readable and self-migrate to the compact shape on the next write; Points hydrates compact locked players from the player table before rendering so headshots/logos still display. No schema migration, seed change, new env var, or new release dependency is required. / Points 页 `SQLITE_TOOBIG` 风险已通过写回 `app_state` 前压缩 `lineup_locks_v1` 解决：锁定阵容快照现在只持久化计分/回放所需字段（`id`、`code`、球队、位置、工资、得分窗口、阵容价值、bank），不再保存头像、logo、未来赛程格等展示型资产。现有较大的 lock registry 仍可读取，并会在下一次写入时自动迁移为紧凑格式；Points 渲染前会从球员表重新 hydrate 紧凑锁里的球员，因此头像/logo 仍会正常显示。本次不需要 schema migration、seed 变更、新环境变量或新发布依赖。
- Overall Standing rank arrows now compare the current cumulative rank with the rank before today's gameday points were applied, so green/red movement reflects today's scoring impact on the overall table. No UI copy was added. No schema migration, seed change, new env var, or new release dependency is required. / Overall Standing 的排名箭头现在会比较当前累计排名与扣除今日比赛日得分前的排名，因此绿色/红色变化反映今日得分对总榜排名的影响。没有新增 UI 文案。本次不需要 schema migration、seed 变更、新环境变量或新发布依赖。
- Standing column semantics are now explicit: on the default `Overall` phase, `PTS` shows the current gameday score and `TOT` shows cumulative overall score; on specific Day phases, both `PTS` and `TOT` show that day's score. Day phases still rank by the selected day's points, while Overall ranking remains cumulative. No schema migration, seed change, new env var, or new release dependency is required. / Standing 的列语义现在明确为：默认 `Overall` 阶段下，`PTS` 显示当前比赛日得分，`TOT` 显示累计总分；具体 Day 阶段下，`PTS` 和 `TOT` 都显示该日得分。Day 阶段仍按所选日期得分排名，Overall 排名仍按累计总分。本次不需要 schema migration、seed 变更、新环境变量或新发布依赖。
- Standing freshness now keeps the fast cached response path but queues background refreshes even while a live-polling cache entry is still fresh, invalidates cached Standing payloads after points/ledger/profile score writes, and renders the `PTS` column from selected-phase `phasePoints` before falling back to stored `gamedayPoints`. This fixes cases where `/standing` showed an old daily score while `/points` showed the latest live score. No schema migration, seed change, new env var, or new release dependency is required. / Standing 新鲜度现在仍保留快速缓存返回路径，但在处于实时轮询期的缓存仍然新鲜时也会排队后台刷新；得分、ledger 或资料分数写入后会清掉 Standing payload 缓存；前端 `PTS` 列会优先渲染选中阶段的 `phasePoints`，再回退到存储的 `gamedayPoints`。这修复了 `/standing` 显示旧每日分数而 `/points` 已显示最新实时分数的情况。本次不需要 schema migration、seed 变更、新环境变量或新发布依赖。
- Player Selection now filters the market and team dropdown to teams still alive in the playoff run, including already-advanced series winners whose next matchup is not yet listed by the NBA feed; existing-roster hydration stays unfiltered. The live seed path uses the same alive-team logic for `can_select` / `can_transact`. No schema migration, env var, dependency, or manual release step is required. / Player Selection 现在会把市场球员和球队下拉限制在仍在季后赛中的球队内，包括已经提前晋级但 NBA feed 尚未列出下一轮赛程的系列赛胜者；现有阵容 hydrate 逻辑仍不做过滤。live seed 路径也用同一套存活球队逻辑写入 `can_select` / `can_transact`。本次不需要 schema migration、环境变量、依赖或人工发布步骤。
- Schedule normalization now filters unstarted NBA `ifNecessary` playoff placeholders, so tentative G5/G6/G7 dates do not create impossible back-to-backs or false future games for teams like Thunder/Suns before those games are confirmed. The same filter is applied to live schedule display, cached next-matchup fallback, and live seed generation. / 赛程归一化现在会过滤尚未开赛的 NBA `ifNecessary` 季后赛占位，因此在 G5/G6/G7 尚未确认前，不会为雷霆/太阳等球队生成不合理的背靠背或虚假的未来比赛；同一过滤也应用于实时赛程展示、缓存 next-matchup 兜底和 live seed 生成。
- Other subpages now avoid duplicate initial loads: frontend GET requests with the same cache key share one in-flight promise, `/transactions` reuses the `market` returned by `/transactions/options` for the default player list instead of immediately calling `/players`, and backend player-data meta is cached per Worker isolate for 5 minutes. No schema migration or seed change is required. / 其他子页面现在会避免重复首屏加载：前端相同 cache key 的 GET 请求会共享同一个进行中的 promise，`/transactions` 默认球员列表会复用 `/transactions/options` 返回的 `market`，不再立刻额外请求 `/players`，后端 player-data meta 会在每个 Worker isolate 内缓存 5 分钟。本次不需要 schema migration 或 seed 变更。
- `/api/standings` de-duplicates in-flight backend refreshes per isolate and rebuilds all phase cache entries in one pass. Fresh cache still returns immediately, while stale/missing cache waits briefly for the shared refresh and then falls back with a short retry if needed. No schema migration or seed change is required. / `/api/standings` 会在同一 isolate 内去重正在进行的后端刷新，并在一次任务里重建所有 phase 缓存。新鲜缓存仍立即返回；过期或缺失缓存会短暂等待共享刷新，必要时再回退并触发短重试。本次不需要 schema migration 或 seed 变更。
- The latest UI change keeps `Daily Fantasy Leaders` at the bottom of `/points`, removes the team abbreviation from each leader card, removes the score prefix label, and renders the score itself in black with a fallback path so it remains visible even if one payload field is missing. / 最新 UI 变更保持 `Daily Fantasy Leaders` 位于 `/points` 页底部，移除了每张 leaders 卡片里的球队缩写，去掉了分数前缀标签，并把实际分数改为黑色，同时保留兜底取值路径，这样即使某个返回字段缺失，分数仍然可见。
- Both `/points` and `/edit-lineup` now use the generated pixel court image as a full adaptive background for the entire `Starting 5` formation area, not just a small decorative center patch: the same portrait asset is extended by layering a `cover` fill underneath a sharper centered court so the background reaches every edge without blank space. Compact bench cards keep the cropped top strip after headshots were removed, and their names are enlarged further so the card surface feels full and visually comfortable. / `/points` 和 `/edit-lineup` 现在都会把生成的像素球场图作为整个 `Starting 5` 阵型区域的自适应完整背景，而不再只是中间一小块装饰：同一张竖版球场图会通过“底层 `cover` 铺满 + 上层清晰居中球场”的叠层方式被扩展到四周边缘，从而避免留白。替补席紧凑卡在去掉头像后继续保留裁短顶部条，同时名字也进一步放大，让卡面更饱满、观感更舒适。
- The `Starting 5` background court has been switched back to a 90-degree horizontal treatment on both `/points` and `/edit-lineup`, with responsive sizing based on the original image ratio so the rotated court stays centered and proportionate across desktop and mobile. / `/points` 和 `/edit-lineup` 的 `Starting 5` 背景球场现已改回旋转 90 度的横向呈现，并按原始图片比例做响应式适配，让旋转后的球场在桌面端和移动端都保持居中且比例协调。
- `Starting 5` cards now keep the player name on a single line with ellipsis truncation when needed, reducing the empty white band under the portrait; the `Next` row shows only the opponent logo (or `-` when missing) instead of a text abbreviation. / `Starting 5` 卡片现在会把球员名字固定为单行展示，超长时用省略号截断，从而收紧头像下方过空的白色区域；`Next` 一行则只显示对手 logo（缺失时显示 `-`），不再显示文字缩写。
- The `Starting 5` background image is temporarily disabled on both `/points` and `/edit-lineup`; the section keeps the same container styling without rendering the rotated pixel-court artwork behind the cards. / `/points` 和 `/edit-lineup` 的 `Starting 5` 背景图现已临时关闭；该区域保留相同容器样式，但不再在卡片后方渲染旋转的像素球场图。
- There is now a repo-level action-shot preview workflow for lineup portraits: `scripts/action_shot_pipeline.py` prints chroma-key prompts from a manifest and batch-postprocesses generated images into transparent PNGs under `frontend/public/nba/action-shots/`. This current path produces photorealistic generated cutouts for fast UI testing, not licensed editorial game photos. / 现在仓库里有一套阵容头像动作照预览工作流：`scripts/action_shot_pipeline.py` 可以根据 manifest 输出纯色抠图底 prompt，并把生成图片批量后处理为 `frontend/public/nba/action-shots/` 下的透明 PNG。当前这条路径产出的是拟真的生成式 cutout，便于快速测试 UI，并不是有授权的真实赛场编辑照片。
